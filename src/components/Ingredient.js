import React from 'react';

const Ingerdient = ({name, amount}) => {
    return (
        <>
            <span>{name} </span>
             
            <span>{amount} </span>
        </>
    );
}

export default Ingerdient;
